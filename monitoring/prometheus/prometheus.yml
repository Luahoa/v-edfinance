global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'vedfinance-api'
    metrics_path: '/api/debug/metrics'
    static_configs:
      - targets: ['host.docker.internal:3000'] # Adjusted for Windows/Mac host access
