# Test environment configuration
# This file is used when running integration tests

# Test Database URL - uses the test database container on port 5434
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5434/vedfinance_test?schema=public

# Use test database for Prisma
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/vedfinance_test?schema=public

# JWT configuration for tests
JWT_SECRET=test-jwt-secret-for-integration-tests-only
JWT_REFRESH_SECRET=test-refresh-secret-for-integration-tests-only

# Disable Redis in tests - use memory cache
REDIS_HOST=localhost
REDIS_PORT=6379

# Test API keys (mocked in tests)
GEMINI_API_KEY=test-gemini-key
STRIPE_SECRET_KEY=sk_test_mock
STRIPE_WEBHOOK_SECRET=whsec_mock

# Test environment
NODE_ENV=test
