/**
 * Calculator utility for testing auto-workflow iterations
 */

export function add(a: number, b: number): number {
  const result = a + b;
  return result;
}

export function multiply(x: number, y: number): number {
  const product = x * y;
  return product;
}
